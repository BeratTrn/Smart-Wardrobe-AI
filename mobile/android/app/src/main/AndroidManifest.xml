<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="internalOnly">
    <!-- installLocation="internalOnly": PackageManager'ın internal/external
         depolama arasında "en uygun" konumu hesaplarken düştüğü bilinen
         emülatör hatasını (INSTALL_FAILED_INSUFFICIENT_STORAGE: "Failed to
         override installation location") önler — bu hesaplamayı devre dışı
         bırakıp doğrudan internal depolamaya kurar. -->

    <!-- İnternet erişimi -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Push bildirimleri (FCM) — Android 13+ (API 33+) için zorunlu,
         olmadan NotificationService.requestPermission() sessizce reddedilir. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Hava durumu için konum izinleri (Geolocator) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Kıyafet çıkarma: canlı kamera önizlemesi (camera paketi) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="smart_wardrobe_ai"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Impeller (Vulkan) yerine Skia/OpenGL kullan — bu emülatör host'ta
             gerçek GPU hızlandırması olmadan SwiftShader (yazılımsal) render
             kullanıyor; Impeller bu kurulumda kararsız (glAttachShader/Vulkan
             hataları, emülatörün kendiliğinden kapanması). Gerçek cihazda
             veya GPU hızlandırması düzgün çalışan bir emülatörde sorun
             yaşanmazsa bu satırı kaldırabilirsin. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Uygulama arka plandayken/kapalıyken gelen FCM bildirimleri bu
             kanalı ve ikonu kullanır — NotificationService'teki
             _androidChannel ile aynı id olmalı. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="smart_wardrobe_high" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
